<h1>Listing Books</h1>
    <table>
        <%= for book <- @books do %>
            <tr>
                <%= render "book.html", book: book %>
                <td><%= link "View", to: Routes.books_path(@conn, :show, book.id) %></td>
            </tr>
        <% end %>
    </table>